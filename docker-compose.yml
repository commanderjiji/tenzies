version: "3"
services:
    vite-app:
        build:
            context: .
        ports:
            - "5173:5173"
        volumes:
            - .:/app
            - /app/node_modules # avoid overwriting node_modules with your host's empty folder
        environment:
            - NODE_ENV=development
