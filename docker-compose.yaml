version: "3.8"

services:
    app:
        build: .
        container_name: tenzies-app
        ports:
            - "5173:5173"
        volumes:
            - .:/app
            - /app/node_modules
        command: npm run dev
